# Предкоммитная проверка ЛНК/ПСТО — обновлено 24.09.2026

**Повторная полная проверка завершена. Новых блокирующих ошибок не выявлено; к локальному коммиту готово. Это не разрешение на push или деплой.**

## Существенные находки

- Исправлен обход проверки дат: прежняя ошибка у одного стыка могла разрешить такую же новую ошибку у другого с одинаковым номером. Проверка теперь учитывает конкретную запись, этап и позицию; интерфейс и сервер используют общую идентификацию ошибки. Три регрессии падали до исправления. На реальной тестовой БД проверены исторический документ и документ с привязкой к циклу: отказ не изменяет стыки, циклы, документ и его состав.
- По согласованию СП-01 при выключенном НК до ТО сохраняет предупреждение о незавершённых обязательных ПСТО/ТВМТ, если внесены данные результата основного НК. Отключённый этап не требуется, одна ранняя заявка предупреждения не создаёт. Текст отличает неподтверждённую историю от фактического пропуска работ. Новые регрессии воспроизводили дефект до исправления.
- Браузерный тест выявил тупик кнопки СП-01: история позволяла исправлять существующий этап, но не создавать отсутствующий. Кнопка теперь открывает штатное внесение нужного этапа; устаревшие этап/цикл проверяются по свежим данным. Проверки дат и серверные запреты не ослаблены.

## Проверено

- Рассмотрены весь незакоммиченный diff и требования исправлений: индивидуальная история вместо наследования по линии, выключение/включение НК до ТО, дозаполнение, ранняя заявка отдельно от результата, сохранение имени при смене даты, защита от конкурирующих изменений. После согласования дополнительно проверены СП-01, обычные/повторные циклы, отмена ПСТО, устаревшие настройки и действия. Руководство обновлено.
- Повторно прошли проверка типов приложения и E2E, **2 730 модульных тестов**, production-сборка и разрешение **110 RPC-ссылок**.
- Полный E2E/интеграционный повтор: **46/46 за 4,7 минуты**, без автоматических повторов. Из ЛНК и ПСТО проверена цепочка СП-01 → сохранённый индекс → поле `dispatcherTasks` → кнопка → внесение ТВМТ → исчезновение предупреждения. Документы ВИК не изменились, документов отключённого этапа не создано.
- Дополнены и отдельно пройдены **2/2 сценария СП-01**: создание отсутствующего этапа открывается штатно; Escape ничего не сохраняет; старая карточка после изменения этапа другим оператором не открывает неверное действие и не перезаписывает данные. Сравниваются стык, циклы, документы и их привязки до/после. Добавлены проверки чужого ID стыка и цикла. Обе временные локальные базы удалены, отсутствие подтверждено.
- В 48 трассировках нет ошибок/предупреждений браузера и полученных HTTP 4xx/5xx. В полном прогоне одна серверная `ECONNRESET` с `unhandled: true`: загрузка отчёта ПСТО оборвалась через 11 мс после перехода в настройки, не при сохранении. В дополнительном прогоне ошибок/обрывов нет. Ошибка не подавлялась; замечание к обработке отменённых запросов остаётся. Прежнее предупреждение сборщика о `manualChunks` также остаётся.
- Реальная SQL/JS-матрица: **162 сравнения**. Матрица диспетчера проверяет 31 цепочку правил и два напоминания: **30 SQL-операций** пересчёта, **один запрос** виртуального поля на страницу.
- В расширенных сценариях СП-01 измерены три загрузки стыка на три нажатия действий, одно сохранение и один пересчёт. Отмена и устаревшая карточка не добавляют сохранений или пересчётов. Повторных мутаций и циклического обновления не обнаружено.

## Нагрузка и границы

- Пройдены регрессионные проверки числа вызовов при открытии/обновлении отчётов и окон, focus/reconnect, пакетной загрузке связей и обновлении диспетчера. Тест на 200 000 объектов в памяти с подменённым SQL-исполнителем подтверждает два пакетных запроса связей при известных настройках; это не нагрузочный прогон PostgreSQL на таком объёме. Реальные SQL-замеры указаны выше.
- Статически проверены изменённые пути: новых таймеров, N+1, запросов внутри цикла по стыкам и немедленной перезагрузки больших неактивных наборов не добавлено. История при переименовании загружается пакетно; проверка одинаковых номеров не добавляет SQL.
- Полный отдельный системный прогон на 200 000 стыков остаётся отложенным по решению пользователя. Время и память полного пересчёта на таком объёме не измерены; фактические записи production не проверялись.
- **Схема и миграции не менялись.** После будущего разрешённого деплоя версии расчётов 39/4 вызовут штатное обновление устаревших индексов и кешей; первый пересчёт добавляет нагрузку. На исторических стыках могут появиться ранее скрытые СП-01. Production, пользовательская локальная БД, коммит, push и деплой не затрагивались.

В этом повторе изменены только регрессионные тесты и отчёт, новая бизнес-логика не добавлялась. Локальные протоколы: `/tmp/welding-precommit-repeat-20260924-uxRJo6/`; предыдущего повтора: `/tmp/welding-sp01-audit-20260924-nCfT35/`.
